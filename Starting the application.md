# To run the application, run the following in bash:

cd "/Users/johanstephan/Programming Projects/Irrigation Scheduling App"
npm run dev

Then open   in your browser.

# Notes
- Sometimes when you run the application and the system still has in memory that http://localhost:5173/ is the URL for the applicaiton, then running the app with the code above will give and error. All you have to do then is either wipe the memory and restart. Or if you just want to open the applicaion as it is, copy the URL and open it in your browser.