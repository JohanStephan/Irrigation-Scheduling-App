{
  "name": "irrigation-scheduling-app",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "DB_PATH=./data/irrigation.db API_PORT=3001 concurrently -n api,web -c blue,green \"npm -w apps/api run dev\" \"npm -w apps/web run dev\"",
    "build": "npm -w apps/api run build && npm -w apps/web run build",
    "test": "npm -w apps/api run test && npm -w apps/web run test",
    "lint": "npm -w apps/api run lint && npm -w apps/web run lint"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
